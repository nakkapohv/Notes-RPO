#Код #Виселица 

```` Python
import random
class R:
    def __init__(self, words: tuple, attempts: int, selected_word: str,j: tuple):
        self.word = words
        self.attempts = attempts
        self.selected_word = selected_word
        self.j = j
    def make_word(self):
        gg = []
        ii =[]
        pp = []
        if len(self.selected_word) > 8:
            print("УДАЧИ! ВАМ НЕ ПОЗАВИДУЕШЬ!")
        print(f"В ВАШЕМ СЛОВЕ {len(self.selected_word)} БУКВ")
        t = [t for t in self.selected_word]
        for i in self.selected_word:
            gg.append("_")
        while True:
            print(f"Кол-во попыток: {self.attempts}")
            print(f"Использованые слова:{pp}\nПИШИ БУКВУ:")
            print(f"-----------------------------------\n{gg}\n-----------------------------------")
            y = str(input())
            pp.append(y)
            if pp.count(y) > 1:
                print("ТЫ ПОВТОРИЛСЯ!!!")
                pp.remove(y)
                
            if len(y) != 1:
                print("Введите ОДНУ букву!")
            else:
                if y in t :
                    print("ДА!")
                    for i in range(t.count(y)):
                        gg.pop(t.index(y))
                        gg.insert(t.index(y),y)
                        if t.count(y) != 1:
                            t.insert(t.index(y),"-")
                            t.remove(y)
                        ii.append("f")
                else:
                    self.attempts = self.attempts - 1
                if self.attempts == 0:
                    print("ВЫ ПРОИГРЛИ!")
                    print(f"Словом было:{self.selected_word}")
                    break
                if len(ii) == len(self.selected_word):
                    print("ВЫ ВЫЙГРАЛИ!")
                    break
            
class A(R):
    pass

with open("words.txt","r",encoding= "UTF-8") as file:
        cont = file.read()
        p= cont.split()
        file.close()

vis = A(p,5,random.choice(p),"")
vis.make_word()
````
