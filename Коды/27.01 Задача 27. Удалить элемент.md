#Код
Имея целочисленный массив и целое число , удаляем все впадения в месте. Порядок расположения элементов может быть изменен. Затем возвращаем число элементов , не равных .numsvalvalnumsnumsval
Рассмотрим количество элементов, в которых не равны , чтобы быть принятым, необходимо выполнить следующие действия:
Изменим массив так, чтобы первые элементы из содержали элементы, не равные . Оставшиеся элементы не так же важны, как и размер .numsknumsvalnumsnums
Вернись.k

```` TypeScript
function removeElement(nums: number[], val: number): number {
    let k = 0; // Указатель на позицию для следующего элемента
    
    for (let i = 0; i < nums.length; i++) {
        if (nums[i] !== val) {
            nums[k] = nums[i]; // Перемещаем элемент в начало
            k++;
        }
    }
    
    return k; // Возвращаем количество элементов, не равных val
}
````

Этот код я взял в готовом варианте, потому что я сам не знаю как это написать, но в целом разобрался:

Сначала мы создаём переменную k, тобеж счётчик результата
Далее задаём цикл for для перебирания каждого элемента, мы перебираем все элементы массива, даже те, которые в итоге “удалятся”
Далее задаём условие if(если), в котором текущий элемент не равен "Val", значит подходит; Копируем подходящий элемент на позицию k в массиве, затем увеличиваем k на 1, чтобы следующий подходящий элемент встал на следующее место. 
Далее если элемент равен, то он нам не подходит  ничего не происходит, пропускается
	И возвращаем только подходящие элементы, нужные, а остальные отбрасываются 